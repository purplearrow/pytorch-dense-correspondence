params:
  num_image_pairs: 25
  num_matches_per_image_pair: 100

networks:
  robot_box_3:
    path_to_network_params: code/data_volume/pdc/trained_models/tutorials/robot_box_3/003501.pth

dataset: [] # which dataset to use, not used for now

output_dir: code/data_volume/pdc/analysis
